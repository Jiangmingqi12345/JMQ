<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sangeng.mapper.ProjectDao">
    <resultMap id="ProjectMap" type="com.sangeng.pojo.Project">
        <id column="id" property="id"/>
        <result column="title" property="title"/>
        <result column="description" property="description"/>
        <result column="teacher_id" property="teacherId"/>
        <result column="status" property="status"/>
        <result column="max_students" property="maxStudents"/>
        <result column="selected_count" property="selectedCount"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <select id="findById" resultMap="ProjectMap">SELECT * FROM project WHERE id=#{id}</select>

    <select id="findAllOpen" resultMap="ProjectMap">
        SELECT * FROM project 
        WHERE status=0 
        <if test="name != null and name != ''">
            AND title LIKE CONCAT('%', #{name}, '%')
        </if>
        ORDER BY create_time DESC LIMIT #{offset}, #{limit}
    </select>

    <insert id="insert" parameterType="com.sangeng.pojo.Project" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO project(title,description,teacher_id,status,max_students,create_time) VALUES(#{title},#{description},#{teacherId},#{status},#{maxStudents},#{createTime})
    </insert>

    <update id="update" parameterType="com.sangeng.pojo.Project">
        UPDATE project SET title=#{title},description=#{description},status=#{status},max_students=#{maxStudents} WHERE id=#{id}
    </update>

    <update id="increaseSelectedCount" parameterType="int">
        UPDATE project SET selected_count = selected_count + 1 WHERE id = #{projectId} AND selected_count &lt; max_students
    </update>
    
    <delete id="deleteById" parameterType="int">
        DELETE FROM project WHERE id = #{id}
    </delete>
    
    <select id="findByTeacher" resultMap="ProjectMap">
        SELECT * FROM project WHERE teacher_id = #{teacherId}
    </select>
    
    <select id="findAll" resultMap="ProjectMap">
        SELECT * FROM project
        <where>
            <if test="name != null and name != ''">
                AND title LIKE CONCAT('%', #{name}, '%')
            </if>
        </where>
        ORDER BY create_time DESC LIMIT #{offset}, #{limit}
    </select>
    
    <update id="updateStatus" parameterType="map">
        UPDATE project SET status = #{status} WHERE id = #{id}
    </update>
    
    <update id="updateSelectedCount" parameterType="map">
        UPDATE project SET selected_count = selected_count + #{delta} WHERE id = #{id} AND selected_count + #{delta} >= 0
    </update>
</mapper>
