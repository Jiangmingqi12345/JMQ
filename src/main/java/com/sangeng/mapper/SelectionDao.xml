<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sangeng.mapper.SelectionDao">
    <resultMap id="SelectionMap" type="com.sangeng.pojo.Selection">
        <id column="id" property="id"/>
        <result column="student_id" property="studentId"/>
        <result column="project_id" property="projectId"/>
        <result column="status" property="status"/>
        <result column="select_time" property="selectTime"/>
        <result column="project_name" property="projectName"/>
        <result column="student_name" property="studentName"/>
    </resultMap>

    <select id="findByStudentProject" resultMap="SelectionMap">
        SELECT s.*, p.title AS project_name, u.name AS student_name 
        FROM selection s 
        LEFT JOIN project p ON s.project_id = p.id 
        LEFT JOIN user_account u ON s.student_id = u.id 
        WHERE s.student_id=#{studentId} AND s.project_id=#{projectId}
    </select>

    <select id="findByStudent" resultMap="SelectionMap">
        SELECT s.*, p.title AS project_name, u.name AS student_name 
        FROM selection s 
        LEFT JOIN project p ON s.project_id = p.id 
        LEFT JOIN user_account u ON s.student_id = u.id 
        WHERE s.student_id=#{studentId}
    </select>

    <insert id="insert" parameterType="com.sangeng.pojo.Selection" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO selection(student_id, project_id, status, select_time) VALUES(#{studentId},#{projectId},#{status},#{selectTime})
    </insert>

    <update id="updateStatus">
        UPDATE selection SET status=#{status} WHERE id=#{id}
    </update>
    
    <select id="findById" resultMap="SelectionMap">
        SELECT s.*, p.title AS project_name, u.name AS student_name 
        FROM selection s 
        LEFT JOIN project p ON s.project_id = p.id 
        LEFT JOIN user_account u ON s.student_id = u.id 
        WHERE s.id=#{id}
    </select>
    
    <select id="findByProject" resultMap="SelectionMap">
        SELECT s.*, p.title AS project_name, u.name AS student_name 
        FROM selection s 
        LEFT JOIN project p ON s.project_id = p.id 
        LEFT JOIN user_account u ON s.student_id = u.id 
        WHERE s.project_id=#{projectId}
    </select>
    
    <select id="findByTeacher" resultMap="SelectionMap">
        SELECT s.*, p.title AS project_name, u.name AS student_name 
        FROM selection s 
        LEFT JOIN project p ON s.project_id = p.id 
        LEFT JOIN user_account u ON s.student_id = u.id 
        WHERE p.teacher_id=#{teacherId}
    </select>
    
    <select id="findAllByStatus" resultMap="SelectionMap">
        SELECT s.*, p.title AS project_name, u.name AS student_name 
        FROM selection s 
        LEFT JOIN project p ON s.project_id = p.id 
        LEFT JOIN user_account u ON s.student_id = u.id 
        WHERE s.status=#{status}
    </select>
    
    <select id="findAll" resultMap="SelectionMap">
        SELECT s.*, p.title AS project_name, u.name AS student_name 
        FROM selection s 
        LEFT JOIN project p ON s.project_id = p.id 
        LEFT JOIN user_account u ON s.student_id = u.id
    </select>
    
    <delete id="deleteById">
        DELETE FROM selection WHERE id=#{id}
    </delete>
    
    <delete id="deleteByProjectId">
        DELETE FROM selection WHERE project_id=#{projectId}
    </delete>
</mapper>
